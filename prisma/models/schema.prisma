generator client {
  provider        = "prisma-client"
  output          = "../generated"
  engineType      = "client"
  runtime         = "bun"
  previewFeatures = ["fullTextSearchPostgres"]
}

datasource db {
  provider = "postgresql"
}
