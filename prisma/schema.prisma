generator client {
  provider        = "prisma-client"
  previewFeatures = ["typedSql","driverAdapters", "queryCompiler"]
  output          = "./generated"

  runtime  = "bun"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
