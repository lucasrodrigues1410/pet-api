generator client {
  provider        = "prisma-client"
  previewFeatures = ["typedSql","fullTextSearchPostgres"]
  output          = "./generated"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
