generator client {
  provider        = "prisma-client"
  previewFeatures = ["typedSql","fullTextSearchPostgres"]
  output          = "./generated"

  runtime  = "bun"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
